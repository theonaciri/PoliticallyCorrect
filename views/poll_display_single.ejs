<md-content md-theme="docs-dark">
    <div layout="column" layout-align="center center">
        <div><h1><%= poll.title %></h1></div>
        <div><h3><pre><%= poll.desc %></pre></h3></div>
    </div>
    <div layout="row">
        <md-divider></md-divider>
        <div flex>
            <md-input-container class="md-block" flex-gt-xs>
                <label>Created on :</label>
                <input disabled><%= poll.createdAt %></input>
            </md-input-container>
        </div>
        <md-input-container class="md-block" flex-gt-xs>
            <label>Last edited on :</label>
            <input disabled><%= poll.updatedAtx %></input>
        </md-input-container>

        <div flex>
            <md-input-container class="md-block" flex-gt-xs>
                <label>Poll start date :</label>
                <input disabled><%= poll.min_date %></input>
            </md-input-container>
        </div>
        <md-input-container class="md-block" flex-gt-xs>
            <label>Poll end date :</label>
            <input disabled><%= poll.max_date %></input>
        </md-input-container>
    </div>
</md-content>

<script>console.log('Hap1');
    var candidates = JSON.parse('<%- candidates %>');
</script>
<!-- LIST OF CANDIDATES -->
<div ng-controller="CanList" class='containerVertical'>
    <md-subheader class="md-no-sticky" ng-controller="DragVoteCtrl">Candidates</md-subheader>
    <% if (typeof candidates !== 'undefined' && candidates !== null) { %>
    <md-list ng-cloak  ng-repeat="can in candidates">
        <md-list-item ng-click="null">
            <div class="_circle" style="background: {{ can.cancolor }}"></div>
            <p>{{ can.name }}</p>
            <h5>{{ can.desc }}</h5>
        </md-list-item>
    </md-list>
    <% } %>
</div>
<p>{{ candidates }}</p>
<!--
<div class='wrapper' ng-controller="DragVoteCtrl">
    <div class='tableRow'>
        <div class='containerVertical'>
            <div ng-repeat="item in '<%= candidates %>'">{{item.content}}</div>
        </div>
    </div>
    <div class="tableRow">
        <div class="container">
            <div>Items1:
                <br/>{{'<%= candidates %>' | json}}</div>
        </div>
    </div>
</div>
-->
